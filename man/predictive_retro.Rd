% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_mcse.R
\name{predictive_retro}
\alias{predictive_retro}
\title{Retrospective predictive replication probabilities against a left-out study}
\usage{
predictive_retro(beta_obs, beta_pred, eps)
}
\arguments{
\item{beta_obs}{Matrix [iterations x chains] of posterior draws of the
left-out study's effect, fit on that study alone.}

\item{beta_pred}{Matrix [iterations x chains] of predicted effects for the
left-out study, drawn from the model fit on the body of evidence. Paired
draw-by-draw with \code{beta_obs}.}

\item{eps}{Practical-relevance threshold on the scale of the effects.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{metric}, \code{p}, \code{k}, \code{n},
\code{ess}, \code{mcse} for the retrospective metrics (\code{P_overall_l}, \code{P_null_l},
\code{P_non_null_l}, \code{P_pos_l}, \code{P_neg_l}).
}
\description{
Paired-indicator replication probabilities that compare the observed effect
of a left-out study with its predicted effect from a body of evidence, each
with a Monte Carlo standard error. This function is agnostic to how many
studies formed the body of evidence (that choice is made upstream when
\code{beta_pred} is produced; see \code{\link[=fit_replicability]{fit_replicability()}}).
}
\details{
\code{beta_obs} and \code{beta_pred} come from disjoint data, so the paired
indicator is a valid estimator. Extract both with \code{permuted = FALSE} (and a
fixed seed for \code{beta_pred}) so the pairing is reproducible.
}

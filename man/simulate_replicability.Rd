% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_replicability}
\alias{simulate_replicability}
\title{Calibrate the metrics by simulation, varying the generative model}
\usage{
simulate_replicability(
  fit,
  data,
  priors = default_priors(),
  eps = NULL,
  vary = c("tau_beta", "tau_alpha", "tau_sig"),
  levels = c(low = 0.1, medium = 0.5, high = 0.9),
  consensus_level = 2,
  min_corroborating = 1,
  R = 30,
  seed = 1000,
  chains = 4,
  iter = 3000,
  warmup = floor(iter/2),
  adapt_delta = 0.99,
  max_treedepth = 15,
  verbose = TRUE
)
}
\arguments{
\item{fit}{A fitted hierarchical model: a \link[rstan:stanfit-class]{rstan::stanfit} object or a
\code{RepliBayes} object (from \code{\link[=fit_replicability]{fit_replicability()}}).}

\item{data}{A data frame with columns \code{study}, \code{x}, \code{m}, giving the study
sample sizes and genotype frequencies to reuse in the simulation.}

\item{priors}{Prior hyperparameters used both as the ground-truth prior of
the varied component and as the prior of the fitted model
(see \code{\link[=default_priors]{default_priors()}}).}

\item{eps}{Practical-relevance threshold; if \code{NULL}, taken from the \code{RepliBayes}
object or computed as 10\\% of the baseline mean outcome.}

\item{vary}{Which generative component to vary across levels: \code{"tau_beta"}
(effect heterogeneity, default), \code{"tau_alpha"} (intercept heterogeneity) or
\code{"tau_sig"} (residual-scale heterogeneity).}

\item{levels}{A named numeric vector of prior percentiles defining the
scenarios (default low/medium/high = 0.10/0.50/0.90).}

\item{consensus_level}{Consensus level(s) for the metrics. Default 2.}

\item{min_corroborating}{Minimum number of other studies required to
corroborate the discovery of the reference study, for the conditional
metrics. Default 1.}

\item{R}{Number of simulated datasets per level.}

\item{seed}{Base seed; replicate \code{r} uses \code{seed + r}.}

\item{chains, iter, warmup, adapt_delta, max_treedepth}{Sampler settings for each
refit.}

\item{verbose}{If \code{TRUE}, print progress messages.}
}
\value{
A tibble with one row per (level, metric): columns \code{level}, \code{pctl},
\code{metric}, \code{value} (mean across converged replicates), \code{mcse}, and \code{n_used}
(number of converged replicates). Metric names are the paper-style names of
\code{\link[=compute_replication_probs_hier]{compute_replication_probs_hier()}}.
}
\description{
Simulation study in which one component of the generative model is varied
while the others are held at their empirical values. For each level, \code{R}
datasets of \code{S} studies are simulated from fixed ground-truth parameters,
each is refit with the hierarchical model, the replication metrics are
computed on the posterior, and they are summarised across replicates with a
Monte Carlo standard error, \eqn{\mathrm{MCSE}(\bar p) = \mathrm{sd}(\hat
p^{(r)})/\sqrt{R_{\mathrm{used}}}}.
}
\details{
The empirical ground truth (study sample sizes, genotype frequencies, and the
generative locations \code{mu_beta}, \code{a}, \code{mu_alpha}, \code{alpha}, \code{mu_sig_m}, \code{sigma_M})
is taken from a fitted hierarchical model; the chosen heterogeneity is then
set to the requested percentiles of its prior.

Only replicates that meet the convergence criterion (no divergences,
Rhat < 1.02, min ESS > 100) contribute to the summaries. The predictor \code{x}
is treated as a genotype dosage in \verb{\{0, 1, 2\}}.
}
\examples{
\dontrun{
data(synthetic_data)
res <- fit_replicability(synthetic_data)
sim <- simulate_replicability(res, synthetic_data, vary = "tau_beta", R = 30)
subset(sim, metric == "P_beta")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_prior}
\alias{sensitivity_prior}
\title{Prior-sensitivity of the replication metrics}
\usage{
sensitivity_prior(
  data,
  priors = default_priors(),
  eps = NULL,
  target = c("tau_beta", "tau_alpha", "tau_sig"),
  percentiles = c(0.1, 0.25, 0.5, 0.75, 0.9),
  k = 2,
  m = 1,
  chains = 4,
  iter_start = 3000,
  iter_max = 24000,
  seed = 42,
  adapt_delta = 0.99,
  max_treedepth = 15,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame with columns \code{study}, \code{x}, \code{m}.}

\item{priors}{Prior hyperparameters (see \code{\link[=default_priors]{default_priors()}}).}

\item{eps}{Practical-relevance threshold; if \code{NULL}, 10\\% of the baseline
(predictor = 0) mean outcome.}

\item{target}{Which heterogeneity's prior to shift: \code{"tau_beta"} (effect,
default), \code{"tau_alpha"} (intercept) or \code{"tau_sig"} (residual scale).}

\item{percentiles}{Prior percentiles at which to place the median of the
shifted prior (0.5 reproduces the main analysis).}

\item{k}{Consensus level(s) passed to \code{\link[=replication_ess]{replication_ess()}}. Default 2.}

\item{m}{Minimum number of other studies agreeing, for the conditional
metrics. Default 1.}

\item{chains, iter_start, iter_max, seed, adapt_delta, max_treedepth}{Sampler and
adaptive-convergence settings.}

\item{verbose}{If \code{TRUE}, print progress messages.}
}
\value{
A list with:
\describe{
\item{\code{metrics}}{a tibble stacking \code{\link[=replication_ess]{replication_ess()}} over the settings,
with extra columns \code{pctl}, \code{mult}, \code{prior_med}, \code{post_med},
\code{iter_used}, \code{converged}, \code{max_rhat}, \code{min_ess}.}
\item{\code{grid}}{one row per setting with the prior grid and diagnostics.}
\item{\code{p_grid}}{a metric-by-percentile matrix of the point estimates for
every metric returned.}
}
}
\description{
Refits the hierarchical model with the data held fixed while the prior on one
between-study heterogeneity is shifted across percentiles of its elicited
prior (its whole location and scale are rescaled so that its median matches
the chosen percentile). Each setting is refit with adaptive convergence and
the replication metrics are recomputed with \code{\link[=replication_ess]{replication_ess()}}.
}
\examples{
\dontrun{
data(synthetic_data)
s <- sensitivity_prior(synthetic_data, target = "tau_beta")
round(s$p_grid, 3)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_hierarchical}
\alias{fit_hierarchical}
\title{Fit the hierarchical (partial-pooling) model}
\usage{
fit_hierarchical(
  data,
  priors = default_priors(),
  chains = 4,
  iter = 3000,
  warmup = floor(iter/2),
  seed = 42,
  adapt_delta = 0.99,
  max_treedepth = 15,
  refresh = 0,
  ...
)
}
\arguments{
\item{data}{A data frame with columns \code{study}, \code{x}, \code{m}.}

\item{priors}{Prior hyperparameters (see \code{\link[=default_priors]{default_priors()}}).}

\item{chains, iter, warmup, seed}{Passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}

\item{adapt_delta, max_treedepth}{NUTS control parameters.}

\item{refresh}{Stan's progress-printing frequency; \code{0} (the default) silences
the per-chain sampler output. Set e.g. \code{refresh = 200} to see progress.}

\item{...}{Further arguments passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}
}
\value{
A \link[rstan:stanfit-class]{rstan::stanfit} object.
}
\description{
Fit the hierarchical (partial-pooling) model
}

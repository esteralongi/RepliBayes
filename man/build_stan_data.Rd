% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_data.R
\name{build_stan_data}
\alias{build_stan_data}
\title{Assemble the Stan data list}
\usage{
build_stan_data(
  data,
  priors = default_priors(),
  model = c("hierarchical", "independence")
)
}
\arguments{
\item{data}{A data frame with (at least) the columns \code{study} (group label),
\code{x} (predictor, e.g. genotype dosage) and \code{m} (outcome).}

\item{priors}{A list of prior hyperparameters, as returned by
\code{\link[=default_priors]{default_priors()}}.}

\item{model}{Either \code{"hierarchical"} (partial pooling, the heterogeneities
\code{tau} are estimated) or \code{"independence"} (the \code{tau} are fixed at the
99.9th percentile of their priors, giving the independence limit).}
}
\value{
A named list suitable as the \code{data} argument of \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.
For the independence model the entries \code{prior_tau_beta_fixed},
\code{prior_tau_alpha_fixed} and \code{prior_tau_sig_fixed} are added.
}
\description{
Builds the \code{data} list for either the hierarchical model or the
independence-limit model from a study-level data frame and a set of prior
hyperparameters.
}

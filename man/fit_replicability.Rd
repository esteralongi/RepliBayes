% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_replicability}
\alias{fit_replicability}
\title{Quantify replicability in one call}
\usage{
fit_replicability(
  data,
  priors = default_priors(),
  eps = NULL,
  consensus_level = 2,
  min_corroborating = 1,
  retrospective = TRUE,
  prospective = TRUE,
  retro_target = NULL,
  retro_evidence = NULL,
  pro_evidence = NULL,
  chains = 4,
  iter = 3000,
  warmup = floor(iter/2),
  seed = 42,
  adapt_delta = 0.99,
  max_treedepth = 15,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame with columns \code{study}, \code{x}, \code{m}.}

\item{priors}{Prior hyperparameters (see \code{\link[=default_priors]{default_priors()}}).}

\item{eps}{Practical-relevance threshold on the scale of the effects. If
\code{NULL} (default) it is set to 10\% of the baseline (predictor = 0) mean
outcome.}

\item{consensus_level}{Consensus level(s) passed to \code{\link[=replication_ess]{replication_ess()}}: at
least \code{consensus_level} of the \code{S} studies agree. A vector is allowed.
Default 2.}

\item{min_corroborating}{Minimum number of \emph{other} studies required to
corroborate the discovery of the reference study, for the per-study
conditional metrics. Default 1.}

\item{retrospective}{If \code{TRUE}, also run the retrospective analysis: predict a
target study from a body of evidence.}

\item{prospective}{If \code{TRUE}, also compute the prospective metrics for a new
study drawn from a body of evidence.}

\item{retro_target}{The study (value of \code{data$study}) to leave out and predict
in the retrospective analysis; defaults to the first study.}

\item{retro_evidence}{The studies forming the body of evidence for the
retrospective prediction (must exclude \code{retro_target}); defaults to all the
other studies. Give a subset of size 1..S-1 to use a smaller evidence base.}

\item{pro_evidence}{The studies forming the body of evidence for the
prospective new study; defaults to all studies. Give a subset of size 1..S.}

\item{chains, iter, warmup, seed, adapt_delta, max_treedepth}{Sampler settings,
passed to \code{\link[=fit_hierarchical]{fit_hierarchical()}} / \code{\link[=fit_independence]{fit_independence()}}.}

\item{verbose}{If \code{TRUE}, print progress messages.}

\item{...}{Further arguments passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}
}
\value{
An object of class \code{"RepliBayes"}: a list with the threshold \code{eps},
the \code{priors}, the fitted models, and the metric tables
\code{metrics_hierarchical}, \code{metrics_independence}, \code{metrics_prospective} and
\code{metrics_retrospective} (each a tibble from \code{\link[=replication_ess]{replication_ess()}},
\code{\link[=predictive_pro]{predictive_pro()}} or \code{\link[=predictive_retro]{predictive_retro()}}).
}
\description{
Fits the hierarchical and independence-limit models and returns all
replication probabilities (empirical, retrospective and prospective), each
with its Monte Carlo standard error.
}
\examples{
\dontrun{
data(synthetic_data)
res <- fit_replicability(synthetic_data)
res
res$metrics_hierarchical
}
}
